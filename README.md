# ARC-dist
adsorption rate constnat distribution
This performs inverse analysis of the rate constant distribution (k_LDF) from experimental uptake data using Tikhonov regularization combined with NNLS or SLSQP optimization, with the optimal regularization parameter (λ) selected via the L-curve method.
